# dbus-broker only watches service directories that exist when it starts, so a
# first Flatpak export into a missing one stays unlaunchable until the next boot.
d /var/lib/flatpak/exports/share/dbus-1/services 0755 root root -
